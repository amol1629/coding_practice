import { createContext } from 'react'

// Create a context for user data
// This context will be used to provide user data to components in the app
const UserContext = createContext()

export default UserContext;


