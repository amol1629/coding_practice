/**
 *  * Rest and Spread Operator : 
 * 
 *  * Rest Operator :
 *      - The rest parameter syntax allows a function to accept an indefinite number of arguments as an array.
 *      - The rest parameter is prefixed with three dots (...)
 *      - The rest parameter must be the last parameter in the function definition.
 *      - The rest parameter collects all the remaining arguments into an array.
 *      - The rest parameter can also be used to collect the arguments of a function.
 *      - The rest parameter can be used to represent an indefinite number of arguments in a function.
 *      - The rest parameter can be used to replace the arguments object.
 *      - The rest parameter can be used to collect the arguments passed to a function.
 *      - The rest parameter can be used to represent an unknown number of arguments.
 * 
 *      - Syntax :
 *              function functionName(...restParameter) {
 *                      // code block
 *              }
 *      
 *    
 *  * Spread Operator :
 *      - The spread operator allows an iterable such as an array expression or string to be expanded in places where zero or more arguments or elements are expected.
 *      - The spread operator is prefixed with three dots (...)
 *      - The spread operator can be used to clone an array.
 *      - The spread operator can be used to concatenate arrays.
 *      - The spread operator can be used to pass an array as arguments to a function.
 *      - The spread operator can be used to convert a string to an array.
 *      - The spread operator can be used to combine objects.
 *      - The spread operator can be used to copy an object.
 *      - The spread operator can be used to merge objects.
 *      - The spread operator can be used to clone an object.
 *      - The spread operator can be used to add properties to an object.
 *      - The spread operator can be used to remove properties from an object.
 *          
 * 
 *          - Syntax :
 *                  const newArray = [...oldArray];
 *                  const newObject = {...oldObject};
 *                  const newArray = [...array1, ...array2];
 *                  const newObject = {...object1, ...object2};
 * 
 *  - Use Cases :
 *          - Rest Operator :
 *                  1) To represent an unknown number of arguments in a function.
 *                  2) To replace the arguments object.
 *                  3) To collect the arguments passed to a function.
 *                  4) To represent an unknown number of arguments.
 *                  5) To collect the elements of an array.
 *                  6) To collect the properties of an object.
 *                  7) To collect the elements of an iterable object.
 *                  8) To collect the values of an object.
 *                  9) To collect the keys of an object.
 *                  10) To collect the entries of an object.
 *                  11) To collect the values of a Map object.
 * 
 *          - Spread Operator :
 *                  1) To clone an array.
 *                  2) To concatenate arrays.
 *                  3) To pass an array as arguments to a function.
 *                  4) To convert a string to an array.
 *                  5) To combine objects.   
 *                  6) To copy an object.
 *                  7) To merge objects.
 *                  8) To clone an object.
 *                  9) To add properties to an object.
 *                  10) To remove properties from an object.
 *                  11) To spread the elements of an array.
 * 
 *      - Differences :
 *                  1) The rest parameter collects all the remaining arguments into an array, while the spread operator expands an array into individual elements.
 *                  
 */

